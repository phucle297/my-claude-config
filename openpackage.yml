name: my-claude-config
version: 1.0.0
description: Structured AI coding workflow — beads, mempalace, omo orchestration for Jira-driven frontend teams
author: phucle297
license: MIT

agents:
  - path: agents/frontend-dev.md
    platforms: [claude, opencode]
  - path: agents/reviewer.md
    platforms: [claude, opencode]
  - path: agents/security-reviewer.md
    platforms: [claude, opencode]
  - path: agents/test-writer.md
    platforms: [claude, opencode]
  - path: agents/debugger.md
    platforms: [claude, opencode]
  - path: agents/planner.md
    platforms: [claude, opencode]
  - path: agents/quality-gate.md
    platforms: [claude, opencode]

skills:
  - path: skills/report-generator/SKILL.md
    platforms: [claude]

mcp:
  mempalace:
    type: local
    command: [mempalace-mcp, --palace, "{env:HOME}/.mempalace/{env:OPENCODE_PROJECT_SLUG}"]
    platforms: [claude, opencode]
  mcp-agent-mail:
    type: remote
    url: "http://127.0.0.1:8765/api/"
    platforms: [claude, opencode]

platforms:
  claude:
    instructions: CLAUDE.md
    settings: settings.json
  opencode:
    instructions: AGENTS.md
    config: opencode.json
